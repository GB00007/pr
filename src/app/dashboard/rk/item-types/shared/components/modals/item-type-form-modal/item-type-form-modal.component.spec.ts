/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ItemTypeFormModalComponent } from './item-type-form-modal.component';

describe('ItemTypeFormModalComponent', () => {
  let component: ItemTypeFormModalComponent;
  let fixture: ComponentFixture<ItemTypeFormModalComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ItemTypeFormModalComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ItemTypeFormModalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/
