/*import { TestBed, inject } from '@angular/core/testing';

import { SnugDialogService } from './snug-dialog.service';

describe('SnugDialogService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SnugDialogService]
    });
  });

  it('should be created', inject([SnugDialogService], (service: SnugDialogService) => {
    expect(service).toBeTruthy();
  }));
});
*/
