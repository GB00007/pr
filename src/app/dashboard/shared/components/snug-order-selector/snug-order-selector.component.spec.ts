/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SnugOrderSelectorComponent } from './snug-order-selector.component';

describe('SnugOrderSelectorComponent', () => {
  let component: SnugOrderSelectorComponent;
  let fixture: ComponentFixture<SnugOrderSelectorComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SnugOrderSelectorComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SnugOrderSelectorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/
