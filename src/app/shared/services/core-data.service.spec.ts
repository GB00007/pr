/*import {
  beforeEachProviders,
  it,
  describe,
  expect,
  inject
} from '@angular/core/testing';
import { CoreDataService } from './core-data.service';

describe('CoreData Service', () => {
  beforeEachProviders(() => [CoreDataService]);

  it('should ...',
      inject([CoreDataService], (service: CoreDataService) => {
    expect(service).toBeTruthy();
  }));
});*/
