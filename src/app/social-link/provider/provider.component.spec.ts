/*import {
  beforeEachProviders,
  describe,
  expect,
  it,
  inject
} from '@angular/core/testing';
import { ProviderComponent } from './provider.component';

beforeEachProviders(() => [ProviderComponent]);

describe('App: DigitalMenu', () => {
  it(
    'should create the app',
    inject([ProviderComponent], (app: ProviderComponent) => {
      expect(app).toBeTruthy();
    })
  );
});
*/
