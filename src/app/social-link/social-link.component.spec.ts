/*import {
  beforeEachProviders,
  describe,
  expect,
  it,
  inject
} from '@angular/core/testing';

import { SocialLinkComponent } from './social-link.component';

beforeEachProviders(() => [SocialLinkComponent]);

describe('App: DigitalMenu', () => {
  it(
    'should create the app',
    inject([SocialLinkComponent], (app: SocialLinkComponent) => {
      expect(app).toBeTruthy();
    })
  );
});
*/
